extends /core/layout

append config
  -
    pageTitle = 'Jumping-links';
    bodyAttributes.class = 'page-jumping-links'

append styles
   +styles(['samples/jumping-links/jumping-links.css'])

append scripts
   +scripts(['samples/jumping-links/jumping-links.js'])

append content
  h1 Jumping-links
