extends /core/layout

append config
  -
    _site.title = 'Test';
    _site.bodyAttributes.class = 'page-test'

//- append styles
   +styles(['samples/jumping-links/jumping-links.css'])

//- append scripts
   +scripts(['samples/jumping-links/jumping-links.js'])

append content
  h1 Test
