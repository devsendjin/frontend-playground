extends /core/layout

append config
  -
    _site.title = 'Jumping-links';
    _site.bodyAttributes.class = 'page-index';


append scripts
   +scripts(['index.js'])

append content
  h1 HTML playground
